# Godot asmdef 属性编辑器实施计划（递进式）

## 1. 文档目标

本计划用于在当前 Godot C# 项目中实现“类似 Unity asmdef”的模块化能力，重点落地以下目标：

- 通过 `*.asmdef` 描述程序集边界与依赖关系
- 通过独立属性编辑器降低配置门槛与错误率
- 在配置变更后自动生成并更新 `csproj` 与宏定义
- 保证方案可扩展、可验证、可持续维护

执行方式采用“**一次只做一件事**”的递进模式：每个阶段通过门禁后再进入下一阶段。

---

## 2. 范围与边界

### 2.1 本轮范围（In Scope）

- 新增 `asmdef` 配置规范（JSON）
- 新增 Godot 编辑器侧属性编辑器（EditorPlugin）
- 新增配置生成器（`asmdef -> csproj`）
- 新增实时更新链路（监听 + 防抖 + 增量更新）
- 新增依赖合法性校验（循环依赖、引用存在性）

### 2.2 非本轮范围（Out of Scope）

- 完整替代 Unity asmdef 全部高级选项
- 跨仓库远程模块管理
- 非 C# 语言模块编译体系

---

## 3. 目标架构

### 3.1 核心组件

- `AsmdefModel`：配置模型（程序集名、引用、宏、平台宏、EditorOnly）
- `AsmdefValidator`：配置与依赖校验器
- `AsmdefGenerator`：`csproj` 文件生成器
- `AsmdefEditorPlugin`：Godot 编辑器插件（属性面板）
- `AsmdefWatcher`：文件监听与防抖更新器

### 3.2 数据流

1. 用户在编辑器中创建或修改 `*.asmdef`
2. 属性编辑器保存并触发校验
3. 校验通过后触发生成器更新目标 `csproj`
4. 生成器增量更新 `ProjectReference` 与 `DefineConstants`
5. 触发 IDE/项目重载提示，完成可见生效

---

## 4. asmdef 规范（最小可用）

### 4.1 字段定义

- `name`：程序集名称（必填）
- `rootNamespace`：根命名空间（必填）
- `references`：依赖模块列表（可选）
- `defines`：通用宏定义列表（可选）
- `platformDefines`：平台宏定义映射（可选）
- `editorOnly`：是否仅编辑器模块（可选，默认 `false`）

### 4.2 示例

```json
{
  "name": "GameFrameX.Entity",
  "rootNamespace": "GameFrameX.Entity",
  "references": ["GameFrameX.Core"],
  "defines": ["GF_ENTITY", "GF_DEBUG_VIEW"],
  "platformDefines": {
    "windows": ["GF_WIN"],
    "android": ["GF_ANDROID"]
  },
  "editorOnly": false
}
```

---

## 5. 递进实施路线（一次一件事）

> 规则：任一阶段未通过“实现 + 验证 + 构建”门禁，不进入下一阶段。

### 阶段 G0：建立最小骨架

**只做这一件事**
- 建立 `asmdef` 模型、读取器、基础目录约定。

**产出**
- `AsmdefModel` 与 JSON 反序列化。
- 最小样例文件与读取命令。

**门禁**
- 样例 `asmdef` 可读取且字段映射正确。

---

### 阶段 G1：实现配置校验器

**只做这一件事**
- 校验配置完整性与依赖合法性。

**产出**
- 名称合法性校验。
- 引用存在性校验。
- 循环依赖检测。

**门禁**
- 非法名称、缺失引用、循环依赖均可被识别并报错。

---

### 阶段 G2：实现 csproj 生成器

**只做这一件事**
- 将 `asmdef` 映射为模块 `csproj` 内容。

**产出**
- `TargetFramework`、`RootNamespace`、`DefineConstants` 生成。
- `ProjectReference` 自动写入。

**门禁**
- 生成 `csproj` 可被 `dotnet build` 正常解析。

---

### 阶段 G3：实现增量更新策略

**只做这一件事**
- 对已有 `csproj` 做“最小变更更新”，避免无关 diff。

**产出**
- 引用增删改增量处理。
- 宏定义去重与稳定排序。

**门禁**
- 同一输入重复生成不产生额外文件差异。

---

### 阶段 G4：实现属性编辑器（EditorPlugin）

**只做这一件事**
- 在 Godot 编辑器内提供 `asmdef` 可视化编辑能力。

**产出**
- 字段编辑 UI（name/references/defines/platformDefines/editorOnly）。
- 即时校验提示与错误高亮。

**门禁**
- 用户不直接编辑 JSON 也可完成创建与配置。

---

### 阶段 G5：实现实时更新链路

**只做这一件事**
- 监听 `asmdef` 变化并自动更新 `csproj`。

**产出**
- 文件监听 + 300~500ms 防抖。
- 变更定位 + 增量生成。

**门禁**
- 保存 `asmdef` 后 1 秒内完成 `csproj` 更新（近实时）。

---

### 阶段 G6：项目重载与体验完善

**只做这一件事**
- 提供“自动/手动重载项目”提示与回退机制。

**产出**
- 更新成功/失败提示。
- 一键重试与错误详情。

**门禁**
- 宏和引用变化后，IDE 可稳定识别最新状态。

---

### 阶段 G7：验收与文档固化

**只做这一件事**
- 形成可复用流程并完成回归验收。

**产出**
- 使用说明、FAQ、故障排查文档。
- 验收记录与后续迭代建议。

**门禁**
- 全流程从“新建模块到构建通过”无手工编辑 `csproj`。

---

## 6. 编辑器交互设计（MVP）

- 新建：在目标目录创建 `*.asmdef`
- 编辑：在 Inspector 面板直接编辑所有核心字段
- 校验：保存时即时执行并展示错误清单
- 生成：通过“保存即生成”更新 `csproj`
- 重载：提示并支持一键重载工程

---

## 7. 风险与对策

- 风险：频繁保存导致重复生成  
  对策：加入防抖、输入哈希、仅在变更时写文件

- 风险：IDE 未即时识别 `csproj` 变化  
  对策：增加重载提示与显式刷新入口

- 风险：模块依赖复杂造成循环引用  
  对策：保存前强校验并阻止落盘

- 风险：团队手改 `csproj` 导致漂移  
  对策：生成器提供 `check` 模式并在 CI 校验一致性

---

## 8. 验收标准（DoD）

必须同时满足：

1. 可在编辑器中创建并修改 `asmdef`
2. 修改后可自动更新目标 `csproj`
3. 依赖关系与宏定义生效且可编译通过
4. 关键异常有可读错误提示与恢复路径
5. 提供完整使用与维护文档

---

## 9. 第一轮执行指令

下一轮从 **G0** 开始，只做：

- 定义 `asmdef` JSON schema 与 `AsmdefModel`
- 建立最小读取与校验入口
- 准备 2 份样例（合法 / 非法）用于验证

通过门禁后，再进入 G1。
